import { fetchSessions } from "../utils/sessions";
import { CardProps } from "../types/sessions/card";
import { Session } from "inspector/promises";
const sessiocard = ({session,mode}:{session:Session,mode:string}) => {
  return (
    <div>
      
    </div>
  )
}

export default sessiocard
